services:
  - type: web
    name: ai-journalist-bot
    env: python
    plan: starter # Or free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --workers 1 --timeout 240 app:app"

